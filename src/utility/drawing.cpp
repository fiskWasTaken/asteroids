//
// Created by fisk on 11/12/17.
//

#include "drawing.h"

namespace drawing {
void drawHealthBar(sf::RenderWindow *renderWindow, int health, int maxHealth) {
//  // health bar
//  auto maxHealth = 100;
//  auto healthPos = sf::Vector2f(pos.x, pos.y + 20);
//  auto healthOrigin = sf::Vector2f(origin.x, origin.y + 20);
//
//  auto healthBar = sf::RectangleShape();
//  auto remainingHealth = sf::RectangleShape();
//
//  remainingHealth.setPosition(healthPos);
//  remainingHealth.setOrigin(healthOrigin);
//  remainingHealth.setSize(sf::Vector2f(health, 10));
//  remainingHealth.setOutlineColor(sf::Color::Transparent);
//  remainingHealth.setFillColor(sf::Color::Red);
//
//  healthBar.setPosition(healthPos);
//  healthBar.setOrigin(healthOrigin);
//  healthBar.setSize(sf::Vector2f(maxHealth, 10));
//  healthBar.setFillColor(sf::Color::Transparent);
//  healthBar.setOutlineColor(sf::Color::White);
//
//  renderWindow->draw(healthBar);
//  renderWindow->draw(remainingHealth);
}
}